"""
The root module of the project, which contains 2 main submodules:
- `api`: A submodule that implements api logic with routers.
- `core`: A central submodule that implements all the business logic of database connection and configuration.
"""
